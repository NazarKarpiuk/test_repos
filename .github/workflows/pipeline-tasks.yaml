name: pipeline-tasks 

on:
  push:
    branches: [main]
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo content
        uses: actions/checkout@v2

      - name: unittest-run
        run: |
          python3 -m unittest
